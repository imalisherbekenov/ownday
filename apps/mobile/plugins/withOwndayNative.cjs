/**
 * The single home for Ownday native configuration. Future widgets must be
 * expressed here (or in sibling config plugins), never in generated projects.
 */
module.exports = function withOwndayNative(config) {
  return config;
};
